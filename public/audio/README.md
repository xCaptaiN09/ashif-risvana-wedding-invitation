# Background music

Add one music file here named exactly `background.mp3`.

The invitation will begin it after the visitor's first scroll, play it quietly, and loop it continuously. To change the soundtrack, replace that file while keeping the same name.
